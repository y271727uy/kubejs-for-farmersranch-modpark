// scripts/creativetab.js

CreativeModeTabEvents.modify(event => {
    // 目标标签是 B（例如你自定义的标签 ID 为 kubejs:my_tab）
    if (event.tab == 'gregtechfoodoption:gregtech_food_option_tab') {
      event.add('kubejs:tungstensteel_apple') // 添加已有物品 A
    }
  })
  
  CreativeModeTabEvents.modify(event => {
    // 目标标签是 B（例如你自定义的标签 ID 为 kubejs:my_tab）
    if (event.tab == 'gregtechfoodoption:gregtech_food_option_tab') {
      event.add('kubejs:tungstensteel_apple') // 添加已有物品 A
    }
  })
  
  